title = "Haskell";

architectures = [ "amd64" ];

is_affected = .source ~ /ghc/
            | .build-depends ~ /ghc/
            | .build-depends ~ /haskell-devscripts/
            | .depends ~ /libghc-/;
is_good = !.debcheck ~ /uninstallable/;
is_bad = .debcheck ~ /uninstallable/;
